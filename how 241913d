[1mdiff --cc brain.py[m
[1mindex 6d33872,e229b2f..0000000[m
[1m--- a/brain.py[m
[1m+++ b/brain.py[m
[36m@@@ -3977,299 -1383,629 +3977,575 @@@[m [mdef update_user_stage(user_id: str, sta[m
          })[m
          return True[m
      except Exception as e:[m
[31m -        print(f"store_ocr_text error: {e}")[m
[32m +        print(f"[S12] update_user_stage error: {e}")[m
          return False[m
  [m
[31m -def search_ocr_documents(user_id, query, limit=3):[m
[31m -    """Search OCR-extracted text for relevant content."""[m
[31m -    print(f"[DEBUG OCR] user={user_id}, query='{query}'")[m
[31m -    if not db:[m
[31m -        print("[DEBUG OCR] db is None")[m
[31m -        return ""[m
[32m +def get_stage_completion_criteria(stage: str, path_name: str) -> str:[m
[32m +    """[m
[32m +    Return the completion criteria for a given stage and path.[m
[32m +    This can be extended with path‑specific criteria.[m
[32m +    """[m
[32m +    criteria_map = {[m
[32m +        "onboarding": "User has provided skills, hours, capital, and target income.",[m
[32m +        "path_selection": "User has selected one income path to pursue.",[m
[32m +        "outreach": "User has sent at least 5 cold outreach messages.",[m
[32m +        "negotiation": "User has received at least one positive response and started price discussion.",[m
[32m +        "first_payment": "User has received their first payment from a client/student.",[m
[32m +        "repeat_customer": "User has at least one repeat client or recurring income."[m
[32m +    }[m
[32m +    # Default fallback[m
[32m +    return criteria_map.get(stage, "Stage complete when user reports progress.")[m
[32m +[m
[32m +def start_income_onboarding(user_id: str, message: str, wave: int = 1) -> dict:[m
[32m +    """[m
[32m +    Progressive onboarding in 3 waves. Each wave asks a small set of questions.[m
[32m +    Returns dict with 'reply' and 'next_wave' (or None when complete).[m
[32m +    """[m
      try:[m
[31m -        docs = list(db.collection("users").document(user_id)[m
[31m -                     .collection("ocr_docs")[m
[31m -                     .order_by("timestamp", direction=firestore.Query.DESCENDING)[m
[31m -                     .limit(20).stream())[m
[31m -        print(f"[DEBUG OCR] Found {len(docs)} OCR documents")[m
[31m -        results = [][m
[31m -        query_words = set(query.lower().split())[m
[31m -        print(f"[DEBUG OCR] Query words: {query_words}")[m
[32m +        profile = get_or_create_income_profile(user_id) or {}[m
          [m
[31m -        for doc in docs:[m
[31m -            data = doc.to_dict()[m
[31m -            text = data.get("text", "")[m
[31m -            print(f"[DEBUG OCR] Text preview: {text[:50]}...")[m
[31m -            text_words = set(text.lower().split())[m
[31m -            overlap = len(query_words & text_words)[m
[31m -            if overlap > 0:[m
[31m -                results.append((overlap, text[:500]))[m
[31m -                print(f"[DEBUG OCR] Keyword match! overlap={overlap}")[m
[32m +        # ── Save response to current wave (if already in progress) ──[m
[32m +        if profile.get('onboarding_wave', 0) > 0 and not profile.get('onboarding_complete'):[m
[32m +            current_wave = profile.get('onboarding_wave', 1)[m
[32m +            save_income_profile(user_id, {[m
[32m +                f"wave_{current_wave}_response": message,[m
[32m +                "onboarding_wave": current_wave + 1[m
[32m +            })[m
[32m +            wave = current_wave + 1[m
[32m +        else:[m
[32m +            # No active wave – start at wave 1[m
[32m +            wave = wave or 1[m
          [m
[31m -        # Vague words detection[m
[31m -        vague_words = ["image", "picture", "screenshot", "this", "that", "see", "look", "upload", "pic", "photo"][m
[31m -        if docs and any(word in query.lower() for word in vague_words):[m
[31m -            most_recent = docs[0].to_dict().get("text", "")[m
[31m -            print(f"[DEBUG OCR] Vague words triggered. Most recent text: {most_recent[:100]}")[m
[31m -            if most_recent:[m
[31m -                results.append((999, most_recent[:500]))[m
[32m +        # ── Wave 1 ──[m
[32m +        if wave == 1:[m
[32m +            if not profile.get('goal'):[m
[32m +                reply = ([m
[32m +                    "Let's start with a few quick questions.\n\n"[m
[32m +                    "1. What do you want to achieve? (e.g., earn extra ₦50k/month, replace my salary)\n"[m
[32m +                    "2. What skills or experience do you have? (e.g., writing, design, teaching)\n"[m
[32m +                    "3. How urgently do you need income? (within 1 month / 3 months / flexible)"[m
[32m +                )[m
[32m +                save_income_profile(user_id, {"onboarding_wave": 1})[m
[32m +                return {"reply": reply, "next_wave": 1}[m
[32m +            else:[m
[32m +                # Already completed wave 1, move to wave 2[m
[32m +                return start_income_onboarding(user_id, message, wave=2)[m
          [m
[31m -        if not results:[m
[31m -            print("[DEBUG OCR] No results found")[m
[31m -            return ""[m
[32m +        # ── Wave 2 ──[m
[32m +        elif wave == 2:[m
[32m +            if not profile.get('available_hours_per_week'):[m
[32m +                reply = ([m
[32m +                    "Great. A few more details to tailor your path:\n\n"[m
[32m +                    "1. How many hours per day can you work on this?\n"[m
[32m +                    "2. Do you have a phone only, or a laptop too?\n"[m
[32m +                    "3. Do you have any budget to start (₦0 is fine)?"[m
[32m +                )[m
[32m +                save_income_profile(user_id, {"onboarding_wave": 2})[m
[32m +                return {"reply": reply, "next_wave": 2}[m
[32m +            else:[m
[32m +                return start_income_onboarding(user_id, message, wave=3)[m
          [m
[31m -        unique_results = [][m
[31m -        seen_texts = set()[m
[31m -        for score, text in results:[m
[31m -            if text not in seen_texts:[m
[31m -                seen_texts.add(text)[m
[31m -                unique_results.append((score, text))[m
[31m -        unique_results.sort(reverse=True)[m
[31m -        print(f"[DEBUG OCR] Returning {len(unique_results)} unique results")[m
[32m +        # ── Wave 3 ──[m
[32m +        elif wave == 3:[m
[32m +            if not profile.get('current_monthly_income'):[m
[32m +                reply = ([m
[32m +                    "Almost done – final questions:\n\n"[m
[32m +                    "1. What's your current monthly income (roughly)?\n"[m
[32m +                    "2. What's your income target?\n"[m
[32m +                    "3. On a scale of 1–10, how confident are you about earning online?\n"[m
[32m +                    "4. What's your internet quality? (good / okay / poor)"[m
[32m +                )[m
[32m +                save_income_profile(user_id, {"onboarding_wave": 3})[m
[32m +                return {"reply": reply, "next_wave": 3}[m
[32m +            else:[m
[32m +                # Onboarding complete[m
[32m +                save_income_profile(user_id, {"onboarding_complete": True, "onboarding_wave": 0})[m
[32m +                return {"reply": "Thanks! Onboarding complete. I'll now recommend your best income path."}[m
          [m
[31m -        combined = "\n---\n".join([t for _, t in unique_results[:limit]])[m
[31m -        return f"📖 From your uploaded past questions:\n{combined}"[m
[32m +        else:[m
[32m +            return {"reply": "Onboarding complete. What would you like to do?"}[m
[32m +    [m
      except Exception as e:[m
[31m -        print(f"[DEBUG OCR] Exception: {e}")[m
[31m -        return ""[m
[32m +        log_error("S12", "start_income_onboarding", e, user_id=user_id)[m
[32m +        return {"rep